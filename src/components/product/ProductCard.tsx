/**
 * ProductCard.tsx
 * 
 * Re-export của ProductCardSimple từ thư mục common.
 * File này được giữ lại để đảm bảo tính tương thích với các file đang import ProductCard từ thư mục product.
 */

import ProductCard from "@/components/common/ProductCardSimple";

export default ProductCard;
